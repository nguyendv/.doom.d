### How to configure doom?
- Use [init.el](init.el) to enable/disable modules only.
- Use [config.el](config.el) for personal configurations.
- Use [packages.el](packages.el) to add non-doom packages.
